# Environment variables — COMMITTED TEMPLATE (no real values here).
# Copy real values into .env.local (gitignored). Never commit secrets.

# ── Supabase ── (Dashboard → Project Settings → API)
NEXT_PUBLIC_SUPABASE_URL=
# Client-safe key: "anon public" (legacy projects) OR "Publishable key" (new projects)
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# SERVER-ONLY — never sent to the browser: "service_role" (legacy) OR "Secret key" (new)
SUPABASE_SERVICE_ROLE_KEY=

# ── Email (Phase 4) ── transactional provider for contact-form notifications
# RESEND_API_KEY=
# CONTACT_NOTIFY_TO=bhavsarpurven515@gmail.com
