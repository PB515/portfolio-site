export const metadata = { title: "Field Notes" };

export default function FieldNotesPage() {
  return (
    <main className="mx-auto max-w-3xl px-6 py-24">
      <h1 className="text-3xl font-semibold tracking-tight">Field Notes</h1>
      <p className="mt-4 text-muted">
        Phase 0 placeholder — published notes (DB-driven), honest
        &ldquo;Writing soon&rdquo; empty-state until filled via admin (Phase 4).
      </p>
    </main>
  );
}
